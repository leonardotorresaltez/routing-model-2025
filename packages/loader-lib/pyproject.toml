[project]
name = "loader-lib"
version = "0.1.0"
description = "Lógica compartida"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0.0",
    "torch==2.6.0",
    "numpy>=1.24.0",
    "pandas==2.2.3"
]


[build-system]
requires = ["poetry-core>=2.0.0"]
build-backend = "poetry.core.masonry.api"